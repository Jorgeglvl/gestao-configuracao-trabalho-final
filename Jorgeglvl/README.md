As variaveis criadas no .env foram: 
DB_HOST
DB_PORT
DB_USER
DB_PASSWORD
APP_ENV=development
Sendo as 4 primeiras variáveis de acesso ao banco de dados e a ultima uma variável de configuração de apicação.